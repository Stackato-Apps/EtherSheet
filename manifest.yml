applications:
- name: ethersheet
  buildpack: https://github.com/cloudfoundry/nodejs-buildpack
  command: cp examples/config-example.js config.js && npm start
  memory: 256M
  services:
  # mysql
  - ethersheet-db
  stack: cflinuxfs2
